<odoo>
<record model="ir.cron" id="planned_product_action">
    <field name="name">Establecer la compañía principal para los contactos</field>
    <field name="model_id" ref="account.model_res_partner"></field>
    <field name="active" eval="True"></field>
    <field name="state">code</field>
    <field name="code">
        # Available variables:
#  - env: Odoo Environment on which the action is triggered
#  - model: Odoo Model of the record on which the action is triggered; is a void recordset
#  - record: record on which the action is triggered; may be void
#  - records: recordset of all records on which the action is triggered in multi-mode; may be void
#  - time, datetime, dateutil, timezone: useful Python libraries
#  - log: log(message, level='info'): logging function to record debug information in ir.logging table
#  - Warning: Warning Exception to use with raise
# To return an action, assign: action = {...}

partners = env['res.partner'].search([])

for partner in partners:
  partner.write({"company_id": 1})
        
    </field>
</record>


<record model="ir.cron" id="planned_contact_action">
    <field name="name">Establecer la compañía principal para los productos </field>
    <field name="model_id" ref="account.model_product_template"></field>
    <field name="active" eval="True"></field>
    <field name="state">code</field>
    <field name="code">
        # Available variables:
#  - env: Odoo Environment on which the action is triggered
#  - model: Odoo Model of the record on which the action is triggered; is a void recordset
#  - record: record on which the action is triggered; may be void
#  - records: recordset of all records on which the action is triggered in multi-mode; may be void
#  - time, datetime, dateutil, timezone: useful Python libraries
#  - log: log(message, level='info'): logging function to record debug information in ir.logging table
#  - Warning: Warning Exception to use with raise
# To return an action, assign: action = {...}

products = env['product.template'].search([])

for product in products:
product.write({"company_id": 1})
        
    </field>
</record>
</odoo>